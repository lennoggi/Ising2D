#!/bin/bash

set -e

simdir="/scratch/cibo/cibo39/Ising2D_run_256_256_beta0.42"
submitscript="HPC6_Intel21.sub"
submit_cmd="qsub"

mkdir ${simdir}
cp ../Ising2D_exe ${simdir}
cp ../SubmitScripts/${submitscript} ${simdir}
cp ../Parameters.hh ${simdir}
cd ${simdir}
${submit_cmd} ${submitscript}

echo ""
echo "Job submitted successfully"
echo ""
